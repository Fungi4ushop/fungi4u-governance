# Status

Last updated: 2026-07-20 (**CO2 spot-checked at ~740 ppm — but the FAE question stays OPEN, and the reason is a trap worth remembering: a morning spot check reads the quiet trough and looks like an all-clear, while the recorded diurnal cycle peaks ~1900 ppm when occupied.** The two support opposite conclusions, so **only the 24h curve — the daily peak and how long it holds — can settle whether the room needs active fresh-air exchange.** The add-on log can't show it (~200 samples ≈ 1–2h retained); use the HA recorder history. Also 2026-07-20: **the Inkbird bridge showed zero errors across that same short window** — encouraging but well short of the "day or two" the reliability call asked for, so it stays open and unspent. **Priority order unchanged, and none of this is at the front of it: 1) buy the harvest scale, 2) Menlo Park Spar / demand, 3) cheap yield levers.** Earlier 2026-07-18: **BOTH fan positions now resolved — in opposite directions.** The **circulation fan is REPLACED and installed: the ACDC Dynamics WF-150** (continuous-duty mixed-flow, 230V induction motor) is now the running circ fan. **The fresh-air fan has now ALSO died and been removed, and its duct is closed** — so **fresh-air exchange / CO2 control is abandoned for now** (accepted gap, not a failure to fix): with no FAE the room's CO2 free-runs — live reading 1563 ppm and expected to climb. Earlier same-day: **circulation-fan replacement bought: ACDC Dynamics WF-150**; now installed. Earlier 2026-07-17: **the circulation fan wore out and died silently, taking RH down for ~24h** — root cause of the low-RH alarm, and the fans turned out to be **the wrong part**: marine bilge blowers rated 600–700h *intermittent*, run 24/7. Fan out, return duct sealed, room on the fresh-air fan alone. Also: the **business-vs-house electricity split is finally measured** — the fruiting room is ~36% of the property, ≈R1,400–1,560/mo, which lands the business near true break-even; and the **"867 W continuous aircon" figure is wrong** and corrected. Earlier 2026-07-16: **the growth gate moved from yield to demand** — first new-client approach, Menlo Park Spar, blocked on a R176 barcode; growth/tempo plan built into `stock-control/finance/CASHFLOW.md`; earlier 2026-07-15: solar/inverter HA-monitoring plan fully scoped into `stock-control/docs/SOLAR.md` and deferred on cost; earlier 2026-07-11: grow-room DHT22 added; fan restore_mode fixed; main router replaced and the HA Pi moved to Ethernet — HA now at 10.0.0.110; monitoring/alarm architecture settled; optimization plan started)

This document replaces `BUSINESS_STATE.md`'s "Phase / Engagement" tracking with a plain answer to: what's actually true right now.

## Business stage

**Priority order — set 2026-07-19, read this first.** The objective is income for the family, via *committable* supply → new clients (see "Sales & growth gate" and `HANDBOOK.md`). Ranked by distance to that objective, front to back:
1. **The harvest scale** — you cannot commit supply you cannot weigh; it's the yield instrument. Buy it.
2. **Demand / new clients** (the Menlo Park Spar step, barcode) — demand is the binding constraint now, not yield.
3. **Cheap yield levers** — the airflow/humidity single-variable experiments + AH/VPD tiles.

Everything else — the wider monitoring build-out (Supabase telemetry, fan tacho, dashboards, the GSM alarm), Inkbird polish, and doc hygiene — is **enabler or insurance behind these, not ahead of them.** Standing test for any task: *does it move committable supply or a client this month?* If not, it waits. **The trap to watch:** microclimate/monitoring work is more tractable and more enjoyable than sales, so it will keep pulling attention — treat that pull as the cue to re-check this order. This reframes the older "microclimate is *the* priority" note below: microclimate is the enabler, not the front of the queue.

The ledger is technically ready for data capture (Stage 1 of the five-stage roadmap below), but **microclimate improvement was previously framed as the current priority, ahead of and independent of that data**. Reasoning: existing experience already indicates the microclimate has enough room for improvement to nearly double yield — confident enough to act on now rather than wait months for ledger-driven measurement to confirm it. The five-stage roadmap describes where the ledger/data side is headed; it isn't a gate the operator is waiting on before improving the physical systems. **The optimization plan itself is in `MICROCLIMATE.md`'s "Optimization plan" section** (started 2026-07-10) — no single symptom is driving it, so it starts with removing confounders and single-variable manual experiments against general grey-oyster fruiting target ranges, before any automation.

1. **Measured Discipline** (ledger side, current) — controlled ledger, flush tracking, processing measurement, manual mobile input.
2. **Stability Monitoring** (ledger side, next) — 4–8 weeks of real data, biological variance analysis, processing drift detection.
3. **Throughput Optimization** — substrate batch comparison, straw/spawn/environment adjustments, waste reduction.
4. **Automation Integration** — environmental telemetry overlay, data-driven environment tuning.
5. **Economic Forecasting** — predictive yield modeling, demand rhythm analysis, capacity planning.

**Actual current focus (2026-07-11): microclimate optimization, sequenced behind a monitoring safeguard.** The DHT22 → RS485 sensor migration is complete and reporting live. The optimization plan (`MICROCLIMATE.md`) now gates crop-risky experiments behind first building a reliable cellular alarm and collecting a clean undisturbed baseline (see its Phase 0/1, and the monitoring architecture in `DECISIONS.md`). Capturing readings into Supabase remains a parallel next step so the environment can be analyzed rather than adjusted by feel alone.

**Added complexity**: the fruiting room and grow room currently share one building, split by an interim divider wall (see `MICROCLIMATE.md` §4 "Interim Divider Wall"). The climate system has to work across this interim two-zone configuration, which makes the rollout more complex than a single dedicated room would be.

## Finance snapshot (2026-07-15)

First cash picture, built from real figures into `stock-control/finance/CASHFLOW.md` (a 13-week cash forecast; money of record stays in Sage/Capitec). Key findings:

- **3 customers, ~31 punnets/week @ R40 → ~R5,370/mo revenue** (Lynnpark 20 cash-on-delivery, Vula 4 monthly, Orchard 6–8 on prompt). ~a quarter of the R20,000/mo objective.
- **Known costs ~R3,100/mo** (Jesca R1,430, Capitec fee R400, substrate R680, punnets R270, gas R320) + minor sundries. **Cash surplus ~R2,000/mo — the operation is cash-positive.**
- **Capitec (R3,407) roughly self-sustains** (Vula+Orchard ≈ Jesca+fee); the **Lynnpark cash (~R800/wk) is the real margin** but is untracked — banking/tracking it is the cash equivalent of the ledger discipline.
- **The ~R34k of personal money that built the operation was mostly one-off *capital*, not running cost** — so the business shouldn't need personal top-up to *operate* going forward, and could send the operator ~R2,000/mo.
- **Electricity — THE BUSINESS SHARE IS NOW MEASURED (2026-07-17), and it lands on the uncomfortable side.** A plug meter carrying the **whole fruiting room** (controller + aircon + humidifier + light) for ~24h read **11.32 kWh**; corrected for the humidifier's abnormal 95% duty during the fan outage (it draws a measured 120 W), a **normal day is ~10.0 kWh ≈ 304 kWh/mo ≈ 36% of the property**, costing **≈R1,400–1,560/mo**. That makes electricity **the single largest cost in the business — bigger than Jesca (R1,430) — and it is absent from `CASHFLOW.md`'s ~R3,100/mo cost list entirely.** Consequence: **the business is near true break-even, not comfortably ~R2,000/mo positive**; most of any transfer to Wessel is **reimbursing power he already pays personally**. This is precisely the reading this document said the split would settle. It does **not** argue for switching things off — revenue per punnet far exceeds the marginal power cost, and the room costs much the same half-full as full, so **filling it is what turns power from a burden into leverage**. It strengthens the demand/Spar case. **Still excluded: the cold-storage fridge** (a business load) — it needs its own 24h reading, worth doing twice over since the tempo plan values gutting it at ~R707/mo. Full numbers: `stock-control/finance/CASHFLOW.md`.
- **Correction: the aircon does NOT draw ~867 W continuously.** That figure (`DECISIONS.md`, 2026-07-16) was **inferred from the inverter feed, which reads the whole property**; 867 W continuous would be 20.81 kWh/day for the aircon alone — **1.8× the entire measured room**. Measured reality: the aircon is **~7.6–8.0 kWh/day, ≈330 W average, ~37% duty, ~70% of the room's power**. It is still the right target, with far less headroom than recorded — and the lever is its duty cycle, not the FAE fan. The FAE CO2 loop stands (it works, and it did not cause the RH event), but its savings case was priced ~2× too high. See `DECISIONS.md` (2026-07-17).
- **Electricity — property-level context (2026-07-16).** With the inverter readable in HA (2026-07-16, see `stock-control/docs/SOLAR.md`), a month of live data shows the whole property draws **~28 kWh/day (~865 kWh/mo)**, of which **~23.5 kWh/day (~715 kWh/mo) is bought from the grid** — costing **≈ R3,200/mo** on the City of Tshwane inclining-block tariff (2026/27), where the **marginal unit is the top block at R5.12/kWh**. Key structural finding: the load is a **near-constant ~1.1 kW base load** (≈26 of the 28 kWh/day) — solar/battery can only nibble at the edges, so **the base load is the real lever** (trimming 300 W continuous ≈ R1,050/mo; likely dominated by winter heating, part of it essential crop-heating). **What's still unmeasured is the business-vs-house split** — closing that is what turns "true profit vs break-even" from estimate to fact. Full savings analysis (tariff, levers, summer-battery plan) belongs in `SOLAR.md`.

## Sales & growth gate (2026-07-16)

**The gate has moved from yield to demand.** The `HANDBOOK.md` objective chain (yield → clients → income) was correct and has now been *walked*, not disproved:

- **Yield was the gate** — new clients couldn't be approached because supply couldn't be committed to them.
- **The microclimate work created the expectation of a bigger yield**, which was enough to act on.
- **So a client was approached: Menlo Park Spar (week of 2026-07-13)** — willing to test grey oyster on their shelves **if there's a barcode on the label**. First new-client approach.
- **Demand is now the binding constraint.** With grey oyster as the only product, the 3 existing customers won't take more however much is grown. Growth needs **new customers**, not more punnets for the old ones. _(Product diversification beyond grey oyster is the other untested demand lever — not yet explored.)_

**The risk this creates:** Spar would be onboarded on an **expectation, not a measurement**. The ledger still has zero production data and **the scale isn't bought**, so the volume being committed to can't be proven. `HANDBOOK.md` is right that what matters is *consistent, committable supply* — failing to supply a Spar is worse than never approaching one. **This makes the scale urgent rather than merely a prerequisite.**

**Barcode: R176 once-off**, a GS1 SA single GTIN, no annual renewal (up to 20 singles before a Company Prefix at R1,964 + R433/yr is worth it). Cheap enough to be a non-decision — but **ask Spar for their full supplier requirement list** (vendor registration, label compliance) before paying for labels; the barcode is unlikely to be the only blocker.

**Fridge:** the cold-storage fridge is **at its limit** — a capacity constraint on growth. ~R8,000 second-hand (2-glass-door shop type). **Storage only, no display needed.** Solid-door is ideal but not worth paying a premium for: gutting a glass unit (disconnect lights ~72W, disable the door-frame anti-condensation heater ~80W, insulate the glass) gets it within ~R61/mo of a solid door and saves ~R707/mo at the R5.12/kWh marginal rate. **Note for the plug-meter sweep: the cold-storage fridge is a *business* load, not a house one** — don't file it in the house column.

Full sequence, costs and tempo: `stock-control/finance/CASHFLOW.md` ("The tempo plan").

## What's working

- **Stock ledger** (Supabase, via `stock-control/index.html`) — built and functional (the panel's substrate → flush → process → sale flow works), but **not actually in use: zero production data captured**. **Now reproducible from the repo** — Phase A complete 2026-07-11: the live schema is captured in a verified migration baseline (`supabase db reset` rebuilds it cleanly) and the remote migration history is reconciled. Remaining before real use: Phase B cleanup and Phase C capture. See the "what's broken/in progress" note below and the roadmap in `stock-control/docs/LEDGER.md`.
- **Fruiting room climate control** (ESPHome) — operational. Confirmed live and reporting on 2026-07-06: ~14.4°C, ~97% RH, circulation fan 85%, fresh-air fan 80%. (Note: the Senseair S8 named in earlier versions of this entry as the planned CO2 sensor was never installed — CO2 sensing came later via the RS485 DFRobot SEN0659, validated 2026-07-09; see `DECISIONS.md`.) Re-confirmed live 2026-07-10 via the Home Assistant MCP integration: CO2 787ppm, 16.2°C, 88.7% RH, circulation fan 81%, fresh-air fan 79%, humidifier on.
- **Home Assistant** — operational, now with an MCP integration allowing an AI coding session to read live entity states directly (set up 2026-07-05/06) rather than relying on screenshots or guesswork. **Now reachable at 10.0.0.110** (changed from 10.0.0.22 on 2026-07-11 — the Pi moved from WiFi to a wired Ethernet connection for reliability; see `DECISIONS.md`). MCP config updated to match, but requires a Claude Code session restart to pick up.
- **RS485 CO2 and temp/humidity sensors (DFRobot SEN0659 + SEN0438)** — validated 2026-07-09. Both reporting live, plausible values together on the shared TB7 bus (CO2 observed 350–1900ppm, RH/Temp tracking the room). Took several days to get working — see `DECISIONS.md`'s 2026-07-09 entries for the root cause (not a hardware fault; ESPHome's stock Modbus component couldn't handle this transceiver's inconsistent self-echo behavior, fixed with a custom raw-UART polling lambda in `fruiting-room-controller.yaml` that scans for a valid frame rather than assuming a fixed offset). Humidifier control now runs off the new RH sensor, replacing the retired DHT22. **DHT22 → RS485 migration is now fully complete** — DHT22 Sensor 2 (TB8/GPIO33) removed from the firmware the same day; its wiring is left physically in place for now (operator's choice).
- **Humidifier duty-cycle monitoring (new, 2026-07-17)** — `sensor.humidifier_duty_1h` / `_24h` (HA `history_stats`), a **"Fruiting room" dashboard** with green/yellow/red gauges (green <70%, yellow 70–90%, red >90%), and an automation alerting above 90% for 1h. Rationale: the relay is only on/off, which hides *how hard* the humidifier is working; healthy operation cycles ~50% duty, and sustained 100% means it cannot reach the 92% off-threshold. In the 2026-07-16/17 event duty pinned at 100% **~20h before** RH fell far enough to trip the `rh_low` alarm — so this is a **leading** indicator where `rh_low` is a lagging one. Monitoring only, in HA not the ESP32 (per `SAFETY.md`); needs no reflash. Mirrored (non-running copy) at `stock-control/home-assistant/humidifier_duty_sensors.yaml`. **Two known limits**: it measures what the ESP32 *commands*, not what the humidifier *does* (the humidifier has its own internal level switch and can be cut off while the relay reads "on"); and HA numeric triggers are **edge-triggered**, so it will not fire for a condition already in progress when it loads — the same trap that disarmed the CO2 alarm leg on 2026-07-15.
- **Humidifier and light relay control** — validated 2026-07-09. Confirmed correctly automated (humidity hysteresis for the humidifier, time schedule for lights) via the external relay multiplug — see `HARDWARE_REFERENCE.md`'s "External relay multiplug" section, newly documented the same day. An initial mixup (humidifier and light plugged into each other's multiplug sockets) made both look like they were behaving backwards; fixed by re-plugging into the correct sockets, no wiring or software change needed.

## What's broken or in progress

- **FANS — both positions now resolved, in opposite directions (2026-07-18).** The original failure (2026-07-17) was the **circulation** fan; the root cause was that **both fans were the WRONG PART** — **SEAFLO SFIB1-130-01 marine *bilge blowers*** (12V, 220 m³/h, rated **600–700h *intermittent* duty**, run here 24/7 = 720 h/month, so the whole rated life is consumed in ~a month). That diagnosis has now played all the way out: the circulation fan is replaced with a continuous-duty part, and the fresh-air fan has since died the same way and been retired.
  - **Circulation fan — REPLACED and installed (2026-07-18): ACDC Dynamics WF-150.** Continuous-duty inline mixed-flow, 230V induction motor, two-speed 45/55W — the continuous-duty part the SEAFLO never was. It is a mains two-speed fan, so it runs **off-controller** (not on a MOSFET/GPIO) — which is why HA's old "Circ Fan" entity now sits idle at its last commanded PWM and no longer reflects this fan. **Confirmed running on its LOW speed** (the intended setting — do not raise it to high; a 150mm mixed-flow on high over-ventilates the 110mm duct and breaks the low-velocity design bet). Full spec/install caveats (150→110 reducers, wire to low speed) in `stock-control/docs/HARDWARE_REFERENCE.md` and the former "Next steps" buy note below.
  - **Fresh-air fan — now ALSO DEAD, REMOVED, and its duct CLOSED (2026-07-18).** It was the same worn-out SEAFLO model at the same 24/7 duty, flagged here on 2026-07-17 as "the bigger risk… currently the room's only fan." It has now failed and been taken out, and the fresh-air duct is closed off. **Consequence: fresh-air exchange and the proportional CO2 loop are gone.** This is an **accepted gap for now, not a repair in progress** (operator's call) — a variable-speed continuous-duty replacement (12V brushless or 0–10V EC fan) was the unresolved hard part, and rather than run a dying fan it's been removed. **CO2 now free-runs** with no active exchange. **Early surprise (2026-07-18): it is FALLING, not climbing** — 1563 → 1502 ppm once the WF-150 circulation fan was mixing the room, the opposite of the predicted rise. Most likely the room is **not airtight** (door/duct/drywall leak), so mixing + passive infiltration removes CO2 faster than the mushrooms produce it; **partly it may also be a measurement shift** (the sensor hangs at the ceiling — a mixed room reads differently from a stratified one). **What matters is where it SETTLES** — sliding to ~800–1000 and holding would mean active FAE isn't needed; a plateau near ~1500 is borderline. **Spot reading 2026-07-20 08:16 SAST: ~740 ppm** (Inkbird, 716–772 across the retained log, 17°C / 88% RH). **This does NOT answer the question — it is the quiet-morning trough, not a settled value.** It matches the ~780 "quiet" figure already recorded on 2026-07-19, and that same entry documents a **diurnal cycle running up to ~1900 ppm when the room is occupied** — i.e. *above* the ~1500 borderline, not below the ~800–1000 all-clear. A morning spot check will always look good and cannot settle this. Grey oyster tolerates elevated CO2, but high CO2 gives long stems / poor caps. HA's "CO² Fan" entity still shows commanded PWM but drives nothing.
  - **How the failures hid (still the key lesson)**: these are **2-wire fans with no tacho — HA shows the *commanded* PWM and cannot tell a spinning fan from a dead one.** The circ fan wore out gradually *while running*; the **2026-07-16 13:52 reflash stopped it** and, worn out, it could not restart. **The reflash broke nothing; it just stopped the fan once.** RH fell ~90% → ~80%, the humidifier pinned at 100% duty, and `rh_low` fired ~20h late pointing at humidity — a symptom. Full detail: `DECISIONS.md` and `stock-control/docs/HARDWARE_REFERENCE.md` ("Fan failure mode: WRONG FAN FOR THE JOB").
  - **DO NOT buy another SFIB1-130-01 for any 24/7 position** — it is a ~1–3 month consumable in this application, forever, in either role.
  - **⚠️ New watch items with the fresh-air fan gone**: (1) **CO2 has no active control** — monitor it and decide whether the FAE replacement is worth doing or whether periodic manual venting / the door suffices; (2) with the duct closed and only the circ fan mixing, **RH should hold** (the earlier duct-open bleed into the grow room is eliminated), but the RH sensor still hangs at ceiling level while the mushrooms sit lower, so **expect some stratification**; (3) **watch the WF-150's induction-motor bearings** over time in the ~97% RH airstream — better than the SEAFLO, not immortal.
  - **Queued: RPM feedback** — a 4-pin PWM fan with tacho on **GPIO21 (free)** makes this alarmable instead of invisible. Needs W30–W35 rewired plus a firmware change; fold it into the Fan 2 rework, since that position is being redesigned anyway.
  - **Until then: after any reboot, reflash or power event, physically confirm both fans are actually turning.** This is now the single most important post-flash check. Do not trust the HA percentage.

- **Stock ledger: reproducible, secured, redesigned — but not yet used with real data.** Reviewed 2026-07-11; **Phases A and B both completed the same day** (all verified via local rebuilds and end-to-end tests). Phase A: live schema captured into a verified migration baseline (repo can now rebuild it). Phase B: dead-code cleanup; **RPC-only security model** (RLS on all tables, narrow SECURITY DEFINER wrappers the only anon write surface — direct table access now blocked); `auth.users` decoupled; and the **harvest redesign** to match the real process — per-picking capture, gap-inferred flush, a batch-removal event (also feeding `v_current_biomass` for CO2 interpretation), and processing that consumes the whole accumulated raw balance; old flush columns / hard-coded biological-yield retired. The panel was reworked to match. Remaining: **Phase C** — disciplined real-data capture, which needs the harvest scale (see next steps) and the weekly per-picking habit. Full detail in `stock-control/docs/LEDGER.md`.
- **Grow-room supply duct not installed.** The plenum feeds two ceiling supply ducts, one per room, but only the fruiting-room side actually exists. The fruiting room's own supply duct is running with its end cap removed (a workaround for CO2/humidity control difficulty, not the original hole-pattern design) — see `MICROCLIMATE.md`.
- ~~**Plenum's fresh-air fan draws outside air indirectly**~~ **— NO LONGER TRUE as of 2026-07-18: the fresh-air fan is dead, removed, and its duct closed (see the fan entry above).** For the record, how it *used* to work: it drew outside air indirectly via a 15mm hole in the grow room's external (rear) dry wall — air entered there, passed through the grow room (picking up that room's aircon-conditioned temperature and CO2 level), then got pulled into the plenum; not a dedicated straight-outside duct. With it gone there is currently **no fresh-air path into the room at all**. The circulation fan (WF-150) is the only fan now; it is a mains two-speed unit, run off-controller, not the old manual PWM.
- **Grow-room DHT22 (monitoring-only) bench-tested, then physically installed in the grow room, 2026-07-10** — reporting via Home Assistant as "Grow Room Temp"/"Grow Room Humidity", on the ~2-3m cable run to its final mounted location. See `DECISIONS.md`.
- **Fan restore_mode fixed 2026-07-10** — circulation/fresh-air fans previously defaulted to off on any firmware reflash (ESPHome's `fan:` default). Now set to `RESTORE_DEFAULT_ON`; confirmed working on the next reflash. See `DECISIONS.md`.

- **Environmental telemetry is not yet flowing into Supabase.** Sensor readings currently go to Home Assistant only. The plan (see Business Stage above) is to also capture them in Supabase so the environment can actually be analyzed, not just observed live. Not yet built.
- **Door sensor and IR transmitter (aircon control) not yet physically installed.** GPIO13/TB10 (door sensor) and GPIO14/TB11 (IR transmitter) are wired/reserved on the controller per `HARDWARE_REFERENCE.md`, but neither device is actually mounted in the room yet. Door-event-triggered behavior and air-conditioner integration both remain not-built until this hardware goes in — see `MICROCLIMATE.md`'s "not automated yet" list.
- **Inkbird CO2/temp/humidity sensor — NOW WORKING (2026-07-19), live in HA** as `sensor.inkbird_co2` / `_temperature` / `_humidity` (first real read: 1074 ppm, 19°C, 84%). It had never actually worked before — three stacked config bugs, all fixed 2026-07-19: (1) the add-on crash-looped on an s6-overlay PID-1 error, fixed with `init: false` in `config.yaml` (needs a version bump to re-ingest); (2) MQTT options were empty, set to `core-mosquitto`/`mqtt-bridge`; (3) the Tuya protocol version was wrong — the device is **3.5**, not the 3.4 recorded on 2026-07-06 (proven by live probing). Full detail: `DECISIONS.md` (2026-07-19). Role unchanged: **secondary/verification only, never control**.
  - **Reliability — RE-OPEN, don't assume it's unreliable.** The old "drops WiFi, needs a physical power-cycle" diagnosis (2026-07-06) predates today's bridge fix — until now the bridge was broken (wrong protocol 3.4 + init crash-loop), so "no data in HA" *looked* like the device vanishing when the device was actually reachable throughout (10.0.0.23, port 6668 open, **continuously mains-powered with a rechargeable battery backup**). So the historical unreliability may simply have been the broken bridge. **Action: observe the now-working bridge for a day or two before spending anything.** **Partial evidence 2026-07-20 (encouraging, not yet conclusive):** the add-on log shows **zero errors, tracebacks, reconnects, timeouts or disconnects** across its retained window — but that window is only ~200 samples (~1–2h), which is far short of the "day or two" this calls for. So: no drop observed, nothing proven. **Leave this open and don't spend yet.** The **smart-plug power-cycle idea is dropped** — it can't reboot a battery-backed device anyway (cutting mains just stops charging; the battery keeps it running). If it genuinely still drops while powered, the lever is **WiFi signal** (~110 ms ping = weakish — reposition / improve AP coverage), not power; recovery would then be a manual reboot, and gaps are acceptable for a secondary sensor. The always-charged battery is a bonus: a mini-UPS that rides through brief power blips.
  - **Still to place physically**: move it to a **lower trolley shelf** to read the top-vs-bottom-shelf gradient the primary sensors (at top-shelf height) can't see — see `HARDWARE_REFERENCE.md` "Sensor placement (physical)".

## Next steps (not yet done, in no particular order)

- **Ledger adoption Phase C: begin disciplined real-data capture** (Phases A and B done; **deployed to production 2026-07-11** — the 6 Phase B migrations were pushed to the live Supabase via `supabase db push` and read-verified: views work, direct table access is 401-blocked. The reworked panel was staged to Google Drive as `mushroom-control.html`; the operator opens it as a local file on the phone, so the final step is loading that new file onto the phone in place of the old one). Production is a clean slate (the one leftover test batch `SUB-2026-W08` was deleted 2026-07-12; open-batches/biomass/snapshot all empty). Phase C then needs the harvest scale (below) and the weekly per-picking habit. Minor deferred polish: rename the legacy `FRUITING_ROOM` raw-stock location (raw is actually cold-stored). See `stock-control/docs/LEDGER.md`.
- ✅ **Circulation fan — DONE 2026-07-18: ACDC Dynamics WF-150 bought AND installed.** (Kept below as the spec/caveat record.) An **inline mixed-flow** duct fan ("inclined flow pressurized duct fan" per its manual — high static pressure, the type wanted for the holed duct), **230V single-phase induction motor** (the continuous-duty motor the SEAFLO never was — this is the actual root-cause fix), **two-speed 45W/55W, 2500/2700 rpm, 70 dB, 150mm ports, 303mm long**. No airflow figure on the plate; a 150mm mixed-flow is typically ~400–550 m³/h free-air. Mains two-speed = drops into **free multiplug socket 1 or 4, no firmware change** (this is Fan 1, the circulation position; it cannot replace Fan 2 / fresh-air, which needs the variable CO2 loop). **Install caveats:** (1) **150mm fan on the 110mm duct → needs a 150→110 reducer each end** (don't leave 150 ports open into the plenum); (2) **wire the plug to L1 (low), not L2 (high)** — the schematic has separate low/high live terminals, and on high a 150mm mixed-flow far exceeds the old 220 m³/h SEAFLO and the low-velocity design bet (also quieter — 70 dB is loud); (3) **check physical clearance** — at 303mm it is much bigger than the little SEAFLO. Running cost ≈ **R168/mo on low** (45W × 24h × R5.12) — a permanent load, slightly above the 12V SEAFLO; book it with the room's power. **Watch the induction motor's bearings over time in ~97% RH** (better than the SEAFLO, not immortal). The 2026-07-17 sweep below is kept as the record of why the RAM route was abandoned.
- ~~**Buy ONE continuous-duty circulation fan (2026-07-17, urgent).**~~ **Superseded 2026-07-18 by the WF-150 purchase above.** The circulation fan was dead and out; the room ran on the fresh-air fan alone. **Do NOT buy another SFIB1-130-01** — it is a marine bilge blower rated 600–700h intermittent, i.e. a ~1–3 month consumable at 24/7. **One, not two**: the fresh-air fan needs variable speed for the CO2 loop and cannot take a 2-speed mains fan.
  - **Sourcing sweep, 2026-07-17 — the RAM 100mm was nationally out of stock**, confirmed sold out the same day at Grow Folk (R899), Homegro Depot (R950), Skyline (R990), Grow Guru (R1,075) and Seeds For Africa (R1,100); Grow Folk's unbranded equivalent (same 199 m³/h, 23/26W) at R699 is also out. **Don't keep working the grow-shop channel** — they all stock the one brand and are all waiting on the same import. Two shops publish no stock status and are worth a call: **Happy Harvest R890** (WhatsApp +27 79 897 8963) and **hydroponic.co.za R970**.
  - **Better lead: Air Movement Supplies (`fans.co.za`)** — an actual HVAC manufacturer and the **Soler & Palau distributor** for SA, with a Sandton branch (collectable from Pretoria). **Johannesburg: 011 608 1749**, rohan@airmove.co.za, Unit B20, Linbro Business Park, 5 Galaxy Ave. Two 100mm mixed-flow options listed: **TD-Silent 250/100** (240/180 m³/h 2-speed, 24/18W, 24/19 dB(A)) and **TD 160/100** (180 m³/h, 35/16W).
  - **Why TD-Silent 250/100 may beat the RAM:** the dead SEAFLO was 220 m³/h, so **the RAM's 199 is actually *under* what the room had**, while the TD-Silent's 180/240 two-speed brackets it. Same wattage class, HVAC-grade rather than a grow-shop house brand, and the motor housing pulls out without disturbing the ducting — which matters in a 97% RH room. **Price unknown — S&P is premium; the call is a quote, not an order.** If it lands under ~R1,500, take it over the RAM; at R2,500+, chase the RAM at Happy Harvest / hydroponic.co.za.
  - **Don't over-read the airflow numbers.** The SEAFLO's "220" is a free-air bilge-blower rating with poor static pressure — through the holed 110mm duct it was likely delivering well under that. A mixed-flow at 199 may move *more* real air than the SEAFLO's "220" did. (Still don't size up to 150mm/580 m³/h — that contradicts the low-velocity design bet.)
- **Fresh-air / CO2 exchange — decide whether to restore it, and how (2026-07-18).** The fresh-air fan is dead, removed, and its duct closed; CO2 control is **abandoned for now** by choice. Open question: does the room actually need active fresh-air exchange, or do periodic manual venting / door openings keep CO2 tolerable? **Watch the CO2 trend first** before spending on a replacement. **Early data point (2026-07-18): with the WF-150 circ fan mixing, CO2 came DOWN (1563 → 1502 ppm), not up** — tentative evidence the room self-vents (leak + mixing) enough that active FAE may be unnecessary, though it may partly be a ceiling-sensor mixing artifact. **Decide on where it settles, not this first drop**: ~800–1000 held = no FAE needed; ~1500 plateau = borderline, revisit. **Still open as of 2026-07-20, and the trap is now named: spot checks cannot answer this.** A morning reading (08:16, ~740 ppm) sits in the quiet trough and looks like an all-clear, while the recorded diurnal cycle peaks ~1900 ppm when occupied — the two readings support opposite conclusions. **What is needed is the 24h curve, specifically the daily peak and how long it holds there** (grey oyster tolerates elevated CO2, but sustained high CO2 gives long stems / poor caps — the peak is what damages the crop, not the mean). **Note the add-on log is the wrong instrument for this** — it retains only ~200 samples (~1–2h), so it structurally cannot show a daily cycle; read the HA recorder history instead (needs a long-lived token, or just the HA dashboard). If a replacement is wanted it needs variable speed for the CO2 loop — a continuous-duty **12V brushless** (keeps the existing MOSFET/PWM wiring) or an **EC fan with 0–10V input** (ESP32 PWM + RC filter — a project). A 2-speed mains fan like the WF-150 **cannot** do this position. Do **not** put another SEAFLO here except as a stopgap.
- **Decide which ESPHome toolchain owns the controller, before the next flash.** The running firmware was built by the **HA addon's ESPHome 2025.10.5**; the laptop has **2026.6.5**, and its build directory holds a stale 07-10 firmware that is *not* what is deployed. Flashing from the laptop would jump ~8 months of ESPHome versions in one step, on a controller whose RS485 sensors depend on a hand-written raw-UART lambda (see `DECISIONS.md`, 2026-07-09). Verified 2026-07-17 that the repo yaml **is** byte-identical to the device's source, so there is no drift to fix — just a version decision to make deliberately rather than by accident.
- **Buy a platform/bench scale (~15–30kg capacity, ±5g) for weighing flush harvests** — **now urgent, not just a prerequisite: it's what turns the expected yield into supply we can commit to a Spar** (see "Sales & growth gate" above). Price it — the R1,500 in `CASHFLOW.md` is a guess. Also an adoption prerequisite: the existing kitchen scale (kept for 4kg substrate bags and 250g punnets) can't weigh a harvest, which exceeds its ~5kg ceiling. This is the yield instrument. See `stock-control/docs/LEDGER.md`'s "Workflow & equipment" section.
- **Plug-meter sweep — step 1 DONE 2026-07-17, remainder outstanding.** ✅ The fruiting room was metered for ~24h: **11.32 kWh (≈10.0 normal)** — see the finance snapshot above. **Note the original instruction here was wrong**: it said to meter "the whole fruiting-room multiplug … = total grow-room draw", but the external relay multiplug only carries the **humidifier and lights** (sockets 1 and 4 are unused) — the **aircon is not on it**, and the aircon is ~70% of the room. What was actually (and correctly) measured was the room's whole feed: controller + aircon + humidifier + light. Meter the *circuit*, not the multiplug. **Remaining**: (1) **the cold-storage fridge — a BUSINESS load** (raw mushrooms are cold-stored, `HANDBOOK.md`), still unmeasured and needed both for the true business total and to price the ~R707/mo gutting saving; (2) then the house's always-on suspects one at a time for ~24h each (house fridge, chest freezer, any heater). **Careful: the cold-storage fridge is a BUSINESS load** (raw mushrooms are cold-stored, `HANDBOOK.md`) — meter it separately and book it to the business, not the house. **Read the kWh over 24h, not instantaneous Watts** (fridges/humidifier/heaters cycle); set the meter's COST to **R5.12/kWh** (marginal rate). **Caveat:** hard-wired loads (geyser, or a hard-wired grow-room heater/aircon) can't be read with a plug meter — those need a clamp meter at the DB. Feed the readings back and they'll be cross-checked against HA's logged total load. Rationale and full savings context in `stock-control/docs/SOLAR.md`.
- **Build the fruiting-room cellular alarm** — a 3-number GSM alarm dialler, battery-backed, wired onto the same circuit breaker as the fruiting-room controller, covering both power-loss (native) and environmental alerts (temp/CO2/RH, via an ESP32 relay into a zone input). **Must be 4G/LTE, not 2G/3G** — SA completes its 2G/3G shutdown by end-2027 (MTN switching off 3G by end-2026), so a 2G unit is a dead-end buy. Leading candidate: **Interlynx S150** (8 digital inputs, backup battery, SMS to 3 numbers); before ordering, confirm the 4G module (not the default 2G), the LTE bands for MTN/Vodacom, and get a price. See `DECISIONS.md` (2026-07-11 device-selection entry). Requires moving the environmental threshold logic into `fruiting-room-controller.yaml`. This is the reliable alarm path; the existing HA phone push (`stock-control/home-assistant/fruiting_room_alarm.yaml`) stays only as a best-effort extra since it dies with the internet. (HA push delivery re-verified working 2026-07-11 after the router/IP changes — via a direct `notify.mobile_app_sm_a226b` test. **2026-07-15: the alarm's CO2 trigger was found silently broken** — it referenced `sensor.fruiting_room_controller_co2`, but the live entity is `sensor.fruiting_room_fruiting_room_controller_co2` (doubled prefix, unlike the RH/temp entities), so HA logged "unknown entity" and the CO2 leg never armed while RH/temp did. Fixed in both `fruiting_room_alarm.yaml` and the live HA automation. Lesson: this alarm is only a documented copy of what's in HA — a repo edit alone doesn't reach the running automation. Note the automation's numeric triggers are edge-triggered: they fire when a value *crosses* a threshold, not whenever it sits beyond one, so it can't be tested by setting a threshold below a stable current reading.) Full design and rationale in `DECISIONS.md` (2026-07-11 monitoring architecture entry). The local buzzer previously considered is dropped — superseded by this device.
- **Set a DHCP reservation for the Home Assistant Pi's new Ethernet MAC address** at 10.0.0.110, so it doesn't shift to a new IP again on a future reboot (see `DECISIONS.md`, 2026-07-11 router entry).
- **Confirm which access point the fruiting-room ESP32 is actually associated with** (believed to be the D-Link extender, not yet verified) — relevant if further WiFi troubleshooting is needed.
- **Consider disabling the TP-Link's 2.4GHz radio** (or otherwise steering devices off it) given its known flakiness — the D-Link extender's 2.4GHz already covers that band reliably.

- **Install the door sensor and IR transmitter** — wiring/terminals already exist (TB10, TB11), just the physical devices need mounting.
- **Re-check whether the Inkbird bridge is actually publishing now**, after the protocol-version fix and redeployment — this was never re-verified in Home Assistant after the fix was applied.
- **Inkbird DHCP address**: on the *old* D-Link router there was a stale unapplied reservation for the Inkbird's MAC (`1c:90:ff:9d:9b:7a`) at `10.0.0.23` vs an actual lease of `10.0.0.102`. That router was replaced by the TP-Link on 2026-07-11, so the old reservation no longer exists; if a fixed Inkbird address is still wanted, set it fresh on the TP-Link. Low priority given the smart-plug plan below makes the exact address less critical.
- ~~**Put the Inkbird on a smart plug controlled by Home Assistant** to auto power-cycle it~~ — **DROPPED 2026-07-19.** The Inkbird has a **rechargeable battery** and is left mains-powered continuously, so cutting the plug won't reboot it (the battery keeps it running). Instead: **observe the now-working bridge for a day or two** — the old "unreliable WiFi" history predates today's bridge fix and was likely the broken bridge, not the device. Only if it genuinely still drops while powered is there a reliability problem, and the lever then is WiFi signal (reposition / AP coverage), not power. See the Inkbird entry above.
- ~~Physically disconnect DHT22 Sensor 2's wiring at TB8~~ — superseded 2026-07-10: TB8/GPIO33 has been reactivated for the new grow-room DHT22 (see "What's working" above), so this wiring is back in active use, not orphaned.
- **Investigate the 30s RS485 poll interval occasionally exceeding ESPHome's component-loop time budget** — seen once: `interval took a long time for an operation (341 ms), max is 340 ms`. Caused by two back-to-back 150ms blocking delays (one per sensor query) in the same interval tick. Hasn't caused a functional failure, but it's right at the warning threshold — if it gets worse (e.g. watchdog resets, WiFi hiccups), consider splitting the two sensor queries across alternating interval ticks instead of querying both every cycle.
- **Build the Supabase environmental telemetry pipeline** referenced above.
- **Liquid culture and grain spawn cultivation stages** are still not documented anywhere (see `HANDBOOK.md`'s production cycle note) — capture this whenever there's time, don't assume it exists.

## Planned future expansion (not started)

Two more physical systems are planned, beyond climate control: **solar** (optimizing use of excess daytime generation) and **water** (the property isn't on municipal supply). Structural pattern already agreed: each gets its own `docs/SOLAR.md` / `docs/WATER.md` in `stock-control`, following the same design-doc + hardware-reference-doc pattern as `MICROCLIMATE.md`/`HARDWARE_REFERENCE.md` — no new repos, no new process.

- **Solar: monitoring is now fully scoped, and deferred on cost (2026-07-15).** `stock-control/docs/SOLAR.md` now exists with a complete, verified build spec for reading the inverter into Home Assistant. The inverter is a **Sunsynk SG01LP1 5kW** (SN 2303132555) with a **BSLBATT PowerLine-5** 5.1kWh battery, in the outbuilding. Current HA state: only the **Sunsynk Power Flow Card** (a HACS dashboard card) is installed — **no data integration**, so the card is empty and nothing solar is exposed to Assist (confirmed 2026-07-15 by listing all HA entities via the REST API). The plan: a **Waveshare RS485-TO-ETH** gateway on the inverter's BMS-port RS485 (via a 2-in-1 splitter, battery keeps its CAN pins) → kellerza Sunsynk add-on → the existing Mosquitto MQTT → HA. Modbus ID `1` verified on the LCD; coexists with the WiFi datalogger so the Sunsynk Connect app is retained. ~R750–1,100 in parts, **not bought** — the operator chose not to spend now. One on-site check outstanding before buying (battery comms = CAN). Full wiring/pinout/config in `SOLAR.md`; rationale in `DECISIONS.md` (2026-07-15). Water remains intent-only, not a spec.

## Possible improvements (not decided, just worth investigating)

- **Ledger ↔ Sage reconciliation.** The Supabase stock ledger records sales as operational *units*, while the money is tracked separately in Sage (accounting) — the two are not connected, so there's no automatic reconciliation between units sold and income recorded (see `HANDBOOK.md`'s "Where things live"). Once Phase C real-data capture is running, connecting them — or at minimum a routine to cross-check ledger sales against Sage income — would catch discrepancies. Not urgent while Sage is underused and the ledger has no real data yet; revisit when both carry real numbers.
- **Automatic end-of-session reminder to update Status/Decisions.** Right now, keeping this document set current relies on remembering to ask "does this need updating?" at the end of a working session. Claude Code supports hooks that could prompt this automatically instead. Not set up — deliberately not added without being asked, since it's one more piece of configuration to maintain — but worth a look if the manual habit turns out to be unreliable in practice.

## Recently cleaned up (2026-07-06)

- Replaced the `operational-core` / `fungi4u-governance` / `mcp-engineering-platform` governance apparatus with this handbook + status + decisions + safety document set (see `HANDBOOK.md` for why).
